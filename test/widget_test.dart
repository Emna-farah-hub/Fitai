// FitAI — basic smoke test placeholder.
// Firebase requires a real device/emulator to run; full integration tests
// will be added in later sprints once the test environment is configured.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder smoke test', (WidgetTester tester) async {
    // TODO: Add widget tests in Sprint 2 after Firebase test mocking is set up.
    expect(true, isTrue);
  });
}
